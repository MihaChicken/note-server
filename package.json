{
  "name": "notes-server",
  "version": "1.0.0",
  "description": "Simple REST API for notes",
  "main": "index.js",
  "scripts": {
    "init:db": "./node_modules/.bin/sequelize init",
    "migrate:db": "./node_modules/.bin/sequelize db:migrate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "miha.chiken@gmail.com",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.2",
    "express": "^4.14.0",
    "express-validation": "^1.0.0",
    "joi": "^9.0.4",
    "sequelize": "^3.24.0",
    "sequelize-cli": "^2.4.0"
  }
}
